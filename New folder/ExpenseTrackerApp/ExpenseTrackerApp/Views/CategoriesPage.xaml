<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="ExpenseTrackerApp.Views.CategoryPage"
    Title="Categories">

    <VerticalStackLayout Padding="10" Spacing="10">

        <!-- Add Category -->
        <Entry Placeholder="Category Name" Text="{Binding NewCategoryName}" />
        <Entry Placeholder="Emoji Icon (e.g. 🍔)" Text="{Binding NewCategoryIcon}" />

        <Button Text="Add Category" Command="{Binding AddCommand}" />

        <!-- Category List -->
        <CollectionView ItemsSource="{Binding Categories}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame BorderColor="Gray" Padding="10" Margin="5">
                        <HorizontalStackLayout Spacing="10">

                            <Label Text="{Binding Icon}" FontSize="20"/>
                            <Label Text="{Binding Name}" VerticalOptions="Center"/>

                            <Button Text="Delete"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteCommand}"
                                    CommandParameter="{Binding Id}" />

                        </HorizontalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>
</ContentPage>